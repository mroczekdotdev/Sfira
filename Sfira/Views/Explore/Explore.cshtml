@{
    @model IEnumerable<PostViewModel>;
    ViewBag.Title = "Explore \u00B7 Sfira";
}

@{await Html.RenderPartialAsync("~/Views/Shared/_CreatePostPartial.cshtml", new PostViewModel()); }

@foreach (var p in Model)
{
    await Html.RenderPartialAsync("~/Views/Shared/_PostPartial.cshtml", p);
}

@section MainAside{
}

@section Aside{
    <section class="aside">
        <header>
            Trending tags
        </header>
        <p>
            Trending content here.
        </p>
    </section>
    <section class="aside">
        <header>
            Trending users
        </header>
        <p>
            Trending content here.
        </p>
    </section>
}
