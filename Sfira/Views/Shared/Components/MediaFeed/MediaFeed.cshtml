@model IEnumerable<AttachmentViewModel>

@if (Model.Any())
{
    foreach (var attachment in Model)
    {
        <section class="item">
            @(await Component.InvokeAsync<AttachmentViewComponent>(attachment))
        </section>
    }
}
